# System Architecture & Flow Diagrams

## 📐 Project Architecture Diagram

```
┌─────────────────────────────────────────────────────────────────┐
│           MULTITHREADED PARALLEL COMPUTATION SYSTEM              │
│                    (ParallelComputationApp)                      │
└─────────────────────────────────────────────────────────────────┘
                                 │
                 ┌───────────────┴───────────────┐
                 │                               │
        ┌────────▼────────┐            ┌────────▼────────┐
        │   GUI LAYER     │            │  COMPUTE LAYER  │
        │   (Swing/AWT)   │            │ (Multithreading)│
        └────────┬────────┘            └────────┬────────┘
                 │                               │
     ┌───────────┴───────────┐       ┌──────────┴──────────┐
     │                       │       │                     │
┌────▼────┐          ┌──────▼──┐   │                     │
│ Input   │          │ Output  │   │                     │
│ Panel   │          │ Panel   │   │                     │
│         │          │         │   │                     │
│• Matrix │          │• Text   │   │                     │
│  Size   │          │  Area   │   │                     │
│• Thread │          │• Progress│   │                     │
│  Count  │          │  Bar    │   │                     │
│• Mode   │          │• Status │   │                     │
│         │          │         │   │                     │
└────┬────┘          └────┬────┘   │                     │
     │                    │         │                     │
┌────▼────────────────────▼────┐   │                     │
│     Control Panel            │   │                     │
│  • Start Button              │   │                     │
│  • Clear Button              │   │                     │
└──────────┬───────────────────┘   │                     │
           │                       │                     │
           │ User Action           │                     │
           │                       │                     │
           └───────────────────────┘                     │
                                   │                     │
                          ┌────────▼─────────┐           │
                          │  SwingWorker     │           │
                          │ (Background Task)│           │
                          └────────┬─────────┘           │
                                   │                     │
                    ┌──────────────┴──────────────┐      │
                    │                             │      │
           ┌────────▼─────────┐        ┌─────────▼──────▼──┐
           │ Matrix Generator │        │   ExecutorService │
           │   (Random Data)  │        │   (Thread Pool)   │
           └──────────────────┘        └─────────┬─────────┘
                                                  │
                                      ┌───────────┴───────────┐
                                      │                       │
                              ┌───────▼──────┐       ┌───────▼──────┐
                              │   Thread 1   │       │   Thread N   │
                              │ (Row 0)      │  ...  │ (Row N-1)    │
                              │              │       │              │
                              │ Computes:    │       │ Computes:    │
                              │ Result[0][j] │       │ Result[n][j] │
                              └───────┬──────┘       └───────┬──────┘
                                      │                       │
                                      └───────────┬───────────┘
                                                  │
                                          ┌───────▼────────┐
                                          │ CountDownLatch │
                                          │ (Wait for all) │
                                          └───────┬────────┘
                                                  │
                                          ┌───────▼────────┐
                                          │ Result Matrix  │
                                          │   Complete     │
                                          └────────────────┘
```

---

## 🔄 Application Flow Diagram

```
START
  │
  ▼
┌─────────────────────┐
│ Launch Application  │
│ (main method)       │
└──────────┬──────────┘
           │
           ▼
┌─────────────────────┐
│ Initialize GUI      │
│ • Create components │
│ • Set layouts       │
│ • Add listeners     │
└──────────┬──────────┘
           │
           ▼
┌─────────────────────┐
│ Display Window      │
│ (setVisible)        │
└──────────┬──────────┘
           │
           ▼
┌─────────────────────┐
│ Wait for User Input │◄──────────┐
└──────────┬──────────┘           │
           │                       │
           │ User clicks           │
           │ "Start"               │
           ▼                       │
┌─────────────────────┐           │
│ Validate Input      │           │
│ • Check numbers     │           │
│ • Check ranges      │           │
└──────────┬──────────┘           │
           │                       │
      Valid? ──No──► Show Error ──┘
           │                       
          Yes                      
           │                       
           ▼                       
┌─────────────────────┐           
│ Disable Start Button│           
│ Clear Output        │           
│ Reset Progress Bar  │           
└──────────┬──────────┘           
           │                       
           ▼                       
┌─────────────────────┐           
│ Create SwingWorker  │           
│ (Background thread) │           
└──────────┬──────────┘           
           │                       
           ▼                       
┌─────────────────────┐           
│ doInBackground()    │           
│ method executes     │           
└──────────┬──────────┘           
           │                       
           ▼                       
┌─────────────────────┐           
│ Generate Matrices   │           
│ • Matrix A (N×N)    │           
│ • Matrix B (N×N)    │           
│ • Random values     │           
└──────────┬──────────┘           
           │                       
           ▼                       
┌─────────────────────┐           
│ Display Samples     │           
│ (3×3 corner)        │           
└──────────┬──────────┘           
           │                       
           ▼                       
    ┌──────┴──────┐               
    │             │               
Multi-Thread?  Single-Thread      
    │             │               
   Yes           No               
    │             │               
    ▼             ▼               
┌──────────┐  ┌─────────┐        
│ Create   │  │ Simple  │        
│ Thread   │  │ Nested  │        
│ Pool     │  │ Loops   │        
└────┬─────┘  └────┬────┘        
     │             │             
     ▼             ▼             
┌──────────┐  ┌─────────┐        
│ Submit   │  │ For i   │        
│ Tasks    │  │   For j │        
│ (Rows)   │  │     For k│        
└────┬─────┘  └────┬────┘        
     │             │             
     ▼             │             
┌──────────┐       │             
│ Threads  │       │             
│ Execute  │       │             
│ Parallel │       │             
└────┬─────┘       │             
     │             │             
     ▼             ▼             
┌──────────────────┴────┐        
│ Wait for Completion   │        
│ (latch.await())       │        
└──────────┬────────────┘        
           │                     
           ▼                     
┌─────────────────────┐          
│ Record Time         │          
│ (executionTime)     │          
└──────────┬──────────┘          
           │                     
           ▼                     
┌─────────────────────┐          
│ Display Results     │          
│ • Result matrix     │          
│ • Execution time    │          
│ • Thread count      │          
└──────────┬──────────┘          
           │                     
           ▼                     
┌─────────────────────┐          
│ Update Progress     │          
│ to 100%             │          
└──────────┬──────────┘          
           │                     
           ▼                     
┌─────────────────────┐          
│ Enable Start Button │          
└──────────┬──────────┘          
           │                     
           ▼                     
┌─────────────────────┐          
│ Wait for Next Input │──────┐   
└─────────────────────┘      │   
           │                 │   
      User clicks            │   
      "Clear"? ──Yes─────────┘   
           │                     
          No                     
           │                     
      User closes               
      window?                   
           │                     
          Yes                    
           │                     
           ▼                     
         EXIT                    
```

---

## 🧵 Thread Execution Flow

```
Main Thread (GUI)
      │
      │ User clicks "Start"
      │
      ▼
┌─────────────────┐
│  SwingWorker    │
│  started        │
└────────┬────────┘
         │
         │ Spawns background thread
         │
         ▼
┌─────────────────┐
│  Background     │
│  Thread         │
└────────┬────────┘
         │
         │ Creates ExecutorService
         │
         ▼
┌──────────────────────────────────────────────┐
│           Thread Pool (Size = N)              │
│                                               │
│  ┌──────────┐  ┌──────────┐  ┌──────────┐   │
│  │ Worker 1 │  │ Worker 2 │  │ Worker N │   │
│  │  IDLE    │  │  IDLE    │  │  IDLE    │   │
│  └──────────┘  └──────────┘  └──────────┘   │
└───────────────────┬──────────────────────────┘
                    │
                    │ Tasks submitted
                    │
         ┌──────────┴──────────┐
         │                     │
         ▼                     ▼
    ┌─────────┐          ┌─────────┐
    │ Task 0  │   ...    │ Task N  │
    │(Row 0)  │          │(Row N)  │
    └────┬────┘          └────┬────┘
         │                    │
         │ Assigned to        │
         │ available          │
         │ workers            │
         ▼                    ▼
┌──────────────────────────────────────────────┐
│           Thread Pool (Size = N)              │
│                                               │
│  ┌──────────┐  ┌──────────┐  ┌──────────┐   │
│  │ Worker 1 │  │ Worker 2 │  │ Worker N │   │
│  │  BUSY    │  │  BUSY    │  │  BUSY    │   │
│  │ Row 0    │  │ Row 1    │  │ Row N-1  │   │
│  └────┬─────┘  └────┬─────┘  └────┬─────┘   │
└───────┼─────────────┼─────────────┼──────────┘
        │             │             │
        │ Computing   │ Computing   │ Computing
        │ result[0][] │ result[1][] │ result[n][]
        │             │             │
        ▼             ▼             ▼
   ┌─────────────────────────────────┐
   │      CountDownLatch             │
   │      (Count = N)                │
   │                                 │
   │  Each thread decrements on      │
   │  completion                     │
   │                                 │
   │  N → N-1 → ... → 1 → 0         │
   └──────────────┬──────────────────┘
                  │
                  │ When count = 0
                  │
                  ▼
          ┌───────────────┐
          │ All threads   │
          │ completed     │
          └───────┬───────┘
                  │
                  ▼
          ┌───────────────┐
          │ Background    │
          │ thread        │
          │ continues     │
          └───────┬───────┘
                  │
                  ▼
          ┌───────────────┐
          │ Display       │
          │ results       │
          │ on GUI        │
          └───────────────┘
```

---

## 📊 Matrix Multiplication Visual

```
Matrix A (N×N)          Matrix B (N×N)        Result Matrix (N×N)
┌─────────────┐       ┌─────────────┐       ┌─────────────┐
│ a00 a01 a02 │       │ b00 b01 b02 │       │ r00 r01 r02 │
│ a10 a11 a12 │   ×   │ b10 b11 b12 │   =   │ r10 r11 r12 │
│ a20 a21 a22 │       │ b20 b21 b22 │       │ r20 r21 r22 │
└─────────────┘       └─────────────┘       └─────────────┘

Row Distribution Among Threads:

Thread 1 ──► Computes Row 0 of Result
              r00 = a00*b00 + a01*b10 + a02*b20
              r01 = a00*b01 + a01*b11 + a02*b21
              r02 = a00*b02 + a01*b12 + a02*b22

Thread 2 ──► Computes Row 1 of Result
              r10 = a10*b00 + a11*b10 + a12*b20
              r11 = a10*b01 + a11*b11 + a12*b21
              r12 = a10*b02 + a11*b12 + a12*b22

Thread 3 ──► Computes Row 2 of Result
              r20 = a20*b00 + a21*b10 + a22*b20
              r21 = a20*b01 + a21*b11 + a22*b21
              r22 = a20*b02 + a21*b12 + a22*b22

All threads work in parallel → Faster completion!
```

---

## 🏗️ Class Structure

```
ParallelComputationApp (extends JFrame)
│
├── Fields (State)
│   ├── GUI Components
│   │   ├── matrixSizeField : JTextField
│   │   ├── threadCountField : JTextField
│   │   ├── outputArea : JTextArea
│   │   ├── progressBar : JProgressBar
│   │   ├── startButton : JButton
│   │   ├── clearButton : JButton
│   │   ├── statusLabel : JLabel
│   │   ├── singleThreadRadio : JRadioButton
│   │   └── multiThreadRadio : JRadioButton
│   │
│   └── Computation Data
│       ├── matrixA : int[][]
│       ├── matrixB : int[][]
│       ├── resultMatrix : int[][]
│       ├── matrixSize : int
│       ├── threadCount : int
│       └── executionTime : long
│
├── Constructor
│   └── ParallelComputationApp()
│       └── Initializes GUI and window properties
│
├── GUI Methods
│   ├── initializeGUI()
│   ├── createTitlePanel() : JPanel
│   ├── createInputPanel() : JPanel
│   ├── createOutputPanel() : JPanel
│   ├── createControlPanel() : JPanel
│   └── createStatusPanel() : JPanel
│
├── Computation Methods
│   ├── startComputation()
│   └── generateMatrices()
│
├── Inner Class: ComputationWorker (extends SwingWorker)
│   ├── doInBackground() : Void
│   │   ├── Generate matrices
│   │   ├── Call single/multi threaded method
│   │   └── Display results
│   ├── process(List<String>)
│   ├── done()
│   ├── performSingleThreadedComputation()
│   ├── performMultiThreadedComputation()
│   └── displayMatrixSample(int[][])
│
├── Inner Class: MatrixMultiplicationTask (implements Runnable)
│   ├── Fields
│   │   ├── row : int
│   │   └── latch : CountDownLatch
│   ├── Constructor(int, CountDownLatch)
│   └── run()
│       └── Computes one row of result matrix
│
└── main(String[])
    └── Launches application on EDT
```

---

## ⚡ Performance Comparison Flow

```
Single-Threaded Execution
═════════════════════════
┌─────┐
│Start│
└──┬──┘
   │
   ▼
┌───────────────────────────────┐
│  One Thread Does ALL Work     │
│                               │
│  Row 0 ──► Row 1 ──► Row 2   │
│   ▼         ▼         ▼       │
│  Row 3 ──► Row 4 ──► Row 5   │
│   ▼         ▼         ▼       │
│  ... ────► ... ────► Row N    │
│                               │
│  Sequential: Slow             │
└───────────┬───────────────────┘
            │
            ▼
       ┌────────┐
       │  End   │
       │Time: T │
       └────────┘


Multi-Threaded Execution (4 threads)
═════════════════════════════════════
┌─────┐
│Start│
└──┬──┘
   │
   ├─────────┬─────────┬─────────┐
   │         │         │         │
   ▼         ▼         ▼         ▼
┌──────┐ ┌──────┐ ┌──────┐ ┌──────┐
│Thread│ │Thread│ │Thread│ │Thread│
│  1   │ │  2   │ │  3   │ │  4   │
├──────┤ ├──────┤ ├──────┤ ├──────┤
│Row 0 │ │Row 1 │ │Row 2 │ │Row 3 │
│Row 4 │ │Row 5 │ │Row 6 │ │Row 7 │
│Row 8 │ │Row 9 │ │Row10 │ │Row11 │
│ ...  │ │ ...  │ │ ...  │ │ ...  │
└───┬──┘ └───┬──┘ └───┬──┘ └───┬──┘
    │        │        │        │
    └────────┴────────┴────────┘
              │
              ▼
         ┌────────┐
         │  End   │
         │Time:T/4│
         └────────┘

Speedup = T / (T/4) ≈ 4x (ideal)
Actual speedup ≈ 3x (accounting for overhead)
```

---

## 🔐 Thread Safety Mechanism

```
Problem: Multiple threads accessing result matrix
═════════════════════════════════════════════════

        Thread 1             Thread 2
            │                    │
            ▼                    ▼
    ┌─────────────┐      ┌─────────────┐
    │ Write to    │      │ Write to    │
    │ result[0][] │      │ result[1][] │
    └──────┬──────┘      └──────┬──────┘
           │                    │
           └─────────┬──────────┘
                     │
                     ▼
            ┌────────────────┐
            │ Result Matrix  │
            │ (Shared)       │
            └────────────────┘

Solution: Row-wise Distribution
════════════════════════════════

✓ Each thread writes to DIFFERENT rows
✓ No two threads access same memory location
✓ No race conditions
✓ No need for locks (efficient!)

Thread 1 → Row 0, 4, 8, ...  (no overlap)
Thread 2 → Row 1, 5, 9, ...  (no overlap)
Thread 3 → Row 2, 6, 10, ... (no overlap)
Thread 4 → Row 3, 7, 11, ... (no overlap)

Result: Thread-safe without synchronization overhead!
```

---

## 📱 GUI Component Hierarchy

```
JFrame (ParallelComputationApp)
│
└── JPanel (mainPanel) [BorderLayout]
    │
    ├── NORTH: titlePanel
    │   └── JLabel (title)
    │
    ├── CENTER: centerPanel [BorderLayout]
    │   │
    │   ├── NORTH: inputPanel [GridBagLayout]
    │   │   ├── JLabel + JTextField (Matrix Size)
    │   │   ├── JLabel + JTextField (Thread Count)
    │   │   └── JLabel + JPanel
    │   │       ├── JRadioButton (Single)
    │   │       └── JRadioButton (Multi)
    │   │
    │   └── CENTER: outputPanel [BorderLayout]
    │       └── JScrollPane
    │           └── JTextArea (output)
    │
    └── SOUTH: bottomPanel [BorderLayout]
        │
        ├── NORTH: controlPanel [FlowLayout]
        │   ├── JButton (Start)
        │   └── JButton (Clear)
        │
        └── SOUTH: statusPanel [BorderLayout]
            ├── NORTH: JLabel (status)
            └── CENTER: JProgressBar
```

---

## 🎯 Event Flow

```
User Interaction → Event → Handler → Action
═════════════════════════════════════════════

1. Start Button Click
   │
   ▼
   ActionEvent
   │
   ▼
   ActionListener.actionPerformed()
   │
   ▼
   startComputation()
   │
   ├─► Validate input
   ├─► Create SwingWorker
   ├─► Execute in background
   └─► Update GUI

2. Clear Button Click
   │
   ▼
   ActionEvent
   │
   ▼
   ActionListener.actionPerformed()
   │
   ▼
   Clear output
   │
   ├─► outputArea.setText("")
   ├─► progressBar.setValue(0)
   └─► statusLabel.setText("Ready")

3. Window Close
   │
   ▼
   WindowEvent
   │
   ▼
   EXIT_ON_CLOSE
   │
   ▼
   Application terminates
```

---

These diagrams provide a comprehensive visual understanding of your project's architecture, flow, and threading mechanism. Include relevant diagrams in your presentation or report for better clarity!

